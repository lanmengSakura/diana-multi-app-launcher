# Changelog

## 0.1.0-beta.4-rc.4 - 2026-09-09

- 修复未安装 Codex 时的状态轮询阻塞：后台读取、120 秒安装检测缓存、并发请求合并和 10 秒 AppX 查询超时；重新检测/挂载时立即刷新安装缓存。
- 轮询按上次完成时间串行安排；普通状态每 5 秒、等待退出时每 2.5 秒；隐藏或操作中暂停，失败时逐步延长到 30 秒；旧目标/旧操作的迟到结果不覆盖新状态。
- 进程检查只取身份/路径/参数；Cursor/Grok 状态读取不再解包美术，Node 能力和 Grok 清单的被动检查最多缓存 60 秒。实际挂载保留独立的新鲜检查，不复用被动缓存作为授权或安全凭据。
- 新增单实例保护和挂载/恢复互斥；重复打开仅唤回窗口。会话状态采用同目录临时文件完整写入后原子替换，失败保留旧记录。
- 移除作者的个人 D 盘候选路径，使用系统安装目录、原有 PATH/环境变量与手动关联；豆包扩展目录按语义版本排序。
- 在入口提示中标明 Cursor/Grok/ZCode 的限定版本，补充豆包小窗口隐藏美术的说明；不改定稿布局、不放宽版本门禁，豆包 `all_frames` 暂未改动。
- 新增可重复的本地便携/NSIS 导出和 SHA-256 清单脚本，以及隔离回归测试；修正旧网页路由测试未声明关联模块依赖的问题。
- 本次提供安装版与便携版，保留既有配乐及授权说明；不重新部署演示站，不扩大真实目标应用的挂载验收范围。

## 0.1.0-beta.4-rc.3 - 2026-09-07

- 状态栏旁新增小号“关联”入口，不挤占状态信息，保留定稿美术和按钮尺寸。
- 支持自动检测、手动选择/粘贴、保存位置、重新检测和逐应用恢复自动识别；旧路径失效不静默切换到另一份安装。
- Codex 按官方安装包定位以跟踪更新；Harness 关联项目根目录，不把文档目录当作应用。
- 修正 Codex 已有原生配色记录时可能跳过新会话风险告知的问题；保存路径与挂载授权分开。
- 保留 rc.2 完整运行组件和既有版本/签名/哈希门禁，保留《Hopeful Dreamer》配乐；公开提供完整功能，用户知情选择。
- 新增路径保存夹具测试和隔离界面/取消授权检查；本次没有重测真实目标应用挂载，不扩大兼容版本承诺。

## 0.1.0-beta.3 - 2026-09-06

- 视觉定稿：保留原立牌、连接臂结构与按钮尺寸，统一珐琅音乐徽章、状态栏内衬、直立字形和按钮材质。
- 主按钮反光仅在悬停时出现；统一关节展开/隐藏交互，播放时向外飘散音符，外框呼吸光晕渐变更自然。
- 统一“原版启动”与“恢复/切回”说明，避免把需要退出再启动的目标描述为热切换。
- 修复 ZCode 运行资源写入层级、Terminal 原版入口、VS Code 旧日间/恢复记录及日间美术蓝图；修复 DeepSeek 链接误打开文件夹并增加服务就绪判断。
- 新增可点击的网页演示；与 EXE 共用定稿界面，挂载状态均为隔离模拟，不访问本机应用。
- 保留 Beta.2 的 LF/SHA-256 资源完整性修复、公开包适配器边界与逐次风险确认。仍为未签名 Beta，不宣称所有目标最新版本均已兼容。

## 0.1.0-beta.2 - 2026-09-02

- 修复 Windows 检出源码时 CSS 从 LF 转为 CRLF，导致 Codex 适配器报 `SHA-256 mismatch` 并在启动前中止的问题。
- 用 `.gitattributes` 固定受保护的 Codex 运行时 CSS 为 LF。
- 新增构建前资源哈希检查、内置字节运行时检查及 Rust 回归测试；主题包不一致时不会再写入用户目录。
- Beta.1 已标记为已知故障版本，用户应升级到 Beta.2。

## 0.1.0-beta.1 - 2026-09-01

- 首个公开多应用测试版，提供可扩展目标列表、日间/暗夜/跟随系统和恢复入口。
- 集成 Codex、豆包、Windows Terminal、VS Code、Cursor、Grok Bot、DeepSeek Harness 与 ZCode 八个目标。
- 保留目标版本、签名、回环监听所有者、渲染器能力与真实挂载状态门禁。
- 不创建 watcher、服务、计划任务、登录启动项或自动更新。
- 公开构建内置 2 分 32.5 秒《Hopeful Dreamer》粉丝向歌词循环，默认关闭并可随时暂停。
- Windows 安装器与便携版暂未签名，发布页提供 SHA-256。
