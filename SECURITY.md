# Security policy

## User choice and distribution

本项目公开提供可选的完整主题启动器，包括版本限定的本地调试挂载组件。用户自行选择是否下载和使用；但下载、安装、打开启动器或保存应用关联都不等于授权调试连接。新开实验调试会话前仍须阅读提示并明确确认，取消不执行本次挂载。曾保存过原生配色也不能替代新的调试授权。

完整版仍受依赖、目标版本、签名、资源完整性和进程归属检查约束；“提供完整组件”不等于承诺任意应用版本均可挂载。公开包不含目标应用、个人适配目录、会话数据或用户路径配置。此处是启动器的公开分发方案，不会改写用户安装的 Skill、平台安全策略或安全提示。

## Experimental local debugger boundary

完整美术挂载可能在用户逐次确认后，以 `127.0.0.1` 随机高位端口启动目标应用的 Chromium 调试接口。回环地址能阻止其他机器直接连接，但不是身份验证：同一账户下的其他本地进程仍可能读取当前可见界面、执行渲染脚本或截图。

结束风险的可靠方法是：撤下视觉层，完整退出所有带调试参数的目标应用进程，再从目标应用官方入口正常启动。仅关闭启动器或仅点击“恢复”不一定立即关闭由目标进程持有的端口。

本项目拒绝非回环地址、监听所有者不匹配、签名/版本门禁失败和未知调试会话。它不创建 watcher、服务、计划任务、自启动项，不修改目标应用安装包、签名或登录态。

## Unsigned beta

`v0.1.0-beta.4-rc.3` 没有商业代码签名证书。只从本仓库的 GitHub Release 下载，并核对发布页 SHA-256。不要从网盘、群文件或重新打包站点运行同名 EXE。

## Private reports

若问题可能暴露本地文件、令牌、会话内容或调试端口信息，请使用 GitHub 私密漏洞报告。日志与 Issue 中不要包含 Cookie、API Key、聊天正文、代码、DOM、截图、WebSocket 地址或未脱敏的用户名路径。
