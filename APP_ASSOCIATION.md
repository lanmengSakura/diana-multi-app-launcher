# 本机应用关联

从 `v0.1.0-beta.4-rc.3` 起提供。rc.2 及更早下载包需手动更新，不会自动获得此入口。

## 怎么使用

1. 在启动器下方选择应用，点击**状态栏旁的“关联”**。
2. 能自动识别时无需设置；找不到或想使用另一份安装时，点击“浏览…”或粘贴完整路径。
3. 点“保存关联”。关闭设置后，再由你主动点击启动器的挂载/原版按钮。选路径或保存均不会启动应用。
4. 下次启动器仍使用保存的位置。应用移动后，重新选择；“恢复自动识别”只取消当前应用的手动关联，不删除程序或皮肤。

| 应用 | 选择内容 |
|---|---|
| Codex | 无需手选 EXE，按 Windows 注册的官方安装包定位；“重新检测”刷新缓存，不绑定更新后会失效的版本目录 |
| Cursor | 安装目录内的 `Cursor.exe` |
| Grok Bot | `Grok Bot.exe` |
| ZCode | `ZCode.exe` |
| 豆包浏览器 | `Doubao.exe`，不是“豆包工作”或安装包 |
| VS Code | `Code.exe` |
| Windows Terminal | `wt.exe` 或 `WindowsTerminal.exe`；不是 `cmd.exe` |
| DeepSeek Harness | 包含 `package.json` 和 `apps/web/package.json` 的项目根目录；不是浏览器、文档目录或 `apps/web` 子目录 |

文件选择窗口不可用时可以直接粘贴路径。取消选择不会保存或清空已有路径；选错程序时保留原关联。支持中文、空格及从资源管理器复制的带引号路径。此入口不接受下载网址、网络共享、命令/参数或安装包。

## 保存和恢复

- 保存于当前用户 `%LOCALAPPDATA%\DianaCodexLauncher\app-links.json`，不写入应用安装目录，也不放在易随启动器更新替换的运行时目录里。
- 优先级：当前应用的手动关联 → 原有环境变量/常见目录/PATH 检测。手动路径失效时停止，提示重新关联；不会默默启动另一份安装。
- 每次成功修改前保留原 JSON 的 `app-links-before-时间戳.json` 备份。写入采用临时文件替换；取消、校验失败或保存失败不清空旧设置。
- 设置格式损坏或来自不支持的版本时保留原文件并提示，不擅自重置全部关联。需要手动恢复时，先退出启动器并保留当前 JSON，再使用同目录的历史备份。
- 路径只在本机保存。反馈问题前请遮住包含用户名/私人目录的路径，不要上传整份配置或备份。

## 功能与授权是两步

“已关联”只表示位置通过基础检查，**不表示已挂载、完成依赖安装、获得授权或保证版本兼容**。文件名和文件头检查不能替代签名、来源和兼容性验证；请只选择你信任的官方程序。

关联读取、文件选择和保存不运行所选程序，不调用主题适配器，不关闭应用，不建立 CDP 连接。真正挂载时继续走现有版本/签名/资源检查和相应调试风险确认，关联记录不保存 `experimentalApproved` 或其他授权。

Codex 未被 Windows 注册时需先完成官方安装；Node.js、Harness 依赖/构建和模型配置仍须按对应项目说明准备，本入口不会自动代装。

从 rc.4 起，被动安装检测（包含未找到的结果）缓存约 120 秒，避免未装 Codex 时反复启动 PowerShell；“重新检测”和主动挂载会立即重新查找官方安装。Node.js 状态提示最多缓存 60 秒，重新检测可清除提示缓存；实际执行前仍重新检查依赖。rc.3 及更早 EXE 需手动更新。

自定义盘符/便携位置不依赖作者的个人目录。首次未识别时手动关联一次即可；指定路径不会绕过 Cursor `3.17.21`、Grok Bot `0.28.0`、ZCode `3.6.5.4145` 的完整美术版本限制。

## 开发检查

```powershell
npm run qa:app-links
node node_modules/typescript/bin/tsc --noEmit
cargo test --manifest-path src-tauri/Cargo.toml --lib --locked
```

`qa:app-links` 启动自己的临时回环网页服务和全新无头浏览器，用模拟 IPC 检查界面、取消/保存、重开读取、错误重试、失效路径、逐应用重置及键盘焦点。它不连接已打开的浏览器、真实启动器或目标应用；截图与报告在忽略提交的 `artifacts/app-links/`。

Rust 用独立临时目录与不可执行的文件夹具验证实际文件保存、读取、校验、备份和失败保留。真实应用启动测试仍为忽略状态。本轮未进行目标应用挂载/恢复，Windows 原生文件选择框尚未真机点选；可以手动粘贴完整路径。打包检查和剩余范围见 [本次发布说明](RELEASE_NOTES.md)。
