# 多合一启动器安全边界

当前目录对应 Diana Multi-App Launcher `v0.1.0-beta.4-rc.2`。它是面向公开测试的八目标实验版，不是全场景或跨版本兼容承诺。

## 默认允许的操作

- 识别目标应用的有限安装路径、运行状态、文件版本与签名状态；
- 通过目标应用已有的正式主题入口部署配色或静态资源；
- 将 Diana 共用美术从一份受审阅资源解包到各应用自己的用户目录；
- 仅在用户点击按钮后执行挂载、切换或恢复，不创建后台 watcher、服务、计划任务和自启动项；
- 写入不含会话正文、DOM、截图、Cookie、令牌或 WebSocket 地址的短日志。

## 实验性本地桥接

Cursor、Grok Bot 与 ZCode 的完整视觉需要版本限定的本机适配器。每次打开临时调试端口前，启动器都必须显示风险确认；端口只绑定 `127.0.0.1`，并且只有完整退出目标应用才能关闭。适配器必须复核目标程序、版本、数字签名、主进程、端口所有者和渲染页能力，任一条件不匹配即拒绝接管。

ZCode 信任检查与预检通过启动器明确传入的 Windows PowerShell 路径执行，不依赖桌面进程 PATH；Windows PowerShell 5.1 的模块搜索路径与 Codex 内置 PowerShell 7 隔离，避免把有效签名误报为失败。Codex 包检测、挂载/恢复脚本和本机 Node 适配器使用同一隔离环境；预检仍然只读，不会启动目标应用或打开端口。

Grok Bot 与 Cursor 新增内置审核运行组件，解包到 `%LOCALAPPDATA%\DianaCodexLauncher\multi-app-v1\reviewed-runtimes` 的独立版本目录。仍优先保留已登记旧适配器，避免破坏旧恢复周期；旧用户应先通过旧入口完整恢复，再移走旧登记文件并备份，才能使用新版内置组件。版本目录中的不可变文件按内置字节/哈希核对，不覆盖恢复状态。私有机器适配目录、运行日志、用户状态和 QA 截图不进入发布包。ZCode 也使用内置版本限定桥接代码；公共 Diana 美术从同一份资源表写出，不按适配器重复嵌入。

## 明确禁止

- 不修改目标应用的 EXE、`app.asar`、安装目录或登录态；
- 不接受非回环调试地址，也不接管无法证明由目标主进程持有的端口；
- 不把第三方应用、源码仓库、`node_modules`、API Key、Cookie 或用户会话数据打进启动器；
- 不把“进程已启动”当作“主题已挂载”，只有注入后的结构与模式复核同时通过才显示 `MOUNTED`；
- 不把本机某个已验证版本外推为跨版本兼容。目标应用更新后必须重新验证。

恢复遵循可逆原则：先撤下视觉层，再完整退出实验会话；需要时恢复首次记录的原生外观，并从目标应用原入口验证普通启动不带调试参数。
