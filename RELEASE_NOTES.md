# Diana 多合一启动器 · Beta.4 RC.4

本次为 Issue #1 的性能与交付修正，重点处理未安装 Codex 时的状态查询阻塞。保留本机应用关联、定稿美术、完整运行组件和《Hopeful Dreamer》本地循环配乐。

## 完整功能，由你知情选择

可以自行选择是否使用本启动器。公开包提供完整挂载组件，但下载、打开或关联应用位置不代表同意开启调试连接。Codex、Cursor、Grok Bot、ZCode 新开实验调试会话前会说明风险并等待确认，取消不执行本次挂载。

**CDP 回环端口没有身份认证。** 同机进程可能读取当前可见界面、执行渲染脚本或截图；仅撤下皮肤或关闭启动器不会关闭目标应用持有的端口，必须完整退出该目标应用。请在信任的本机环境使用，并阅读 [完整风险说明](https://github.com/lanmengSakura/diana-multi-app-launcher/blob/main/SECURITY.md)。

## 更新内容

- Codex 和其他目标的状态查询移到后台；未找到 Codex 的结果也缓存 120 秒，并发查询合并，AppX 查询超过 10 秒结束本次查询。
- 自动刷新不再重叠；窗口隐藏、挂载或保存关联期间暂停，失败后逐步放慢重试，切换目标后忽略旧查询结果。
- 降低进程探测成本；Cursor/Grok 状态读取不再反复解包美术资源，Node/Grok 的被动检查缓存 60 秒。实际挂载仍重新核验依赖、完整清单及原有安全条件。
- 新增单实例与操作互斥：重复打开只唤回已有启动器，不再次挂载；会话文件完整写好后再替换，失败保留旧记录。
- 移除作者个人 D 盘安装候选路径，保留系统目录、原有环境变量/PATH 与手动关联。豆包主题目录按语义版本排序。
- 入口标明 Cursor `3.17.21`、Grok Bot `0.28.0`、ZCode `3.6.5.4145` 的限制；补充豆包小窗口隐藏立绘的说明，不改防遮挡布局。
- 新增便携版/NSIS 本地导出和 SHA-256 清单脚本。完整运行组件与风险确认沿用现有版本，不新增 watcher、服务、计划任务或自启动。

## 下载与更新

- 安装版：`Diana-Multi-App-Launcher_0.1.0-beta.4-rc.4_x64-setup.exe`。
- 便携版：`diana-multi-app-launcher_0.1.0-beta.4-rc.4_x64-portable.exe`。
- 核对随附 `SHA256SUMS.txt`；两种 EXE 均未签名。更新前正常关闭旧启动器，保留旧文件以便回退。旧版不会自动更新，新旧版本不要同时运行。
- 本次不迁移旧 Cursor/Grok 登记，不删除旧恢复记录，不更改目标应用设置。已登记适配器的恢复周期仍按原入口完成。
- 选择应用 → 状态栏旁“关联”（需要时）→ 保存路径 → 关闭设置 → 点击主按钮并自行决定是否同意风险提示。

## 检查范围与限制

源码检查为 53 项 Rust 测试通过（另有 1 项真实应用 smoke 保持忽略）、21 项脚本测试通过，以及 TypeScript/生产构建、格式检查与受保护资源哈希检查。查询/轮询使用假查询和假时钟，写入/导出使用独立临时目录与不可执行夹具；这不是在报告者机器上的复测。

没有在报告者 Windows 10 / 8GB 环境复验窗口流畅度，也未新增八目标真实挂载/日夜/恢复测试。Windows 原生文件选择框及新单实例的真机交互仍待验收；安装包生成不等于已在干净机器完成安装/卸载验收。

豆包 `all_frames` 暂保留：目前缺少足够的宿主页/嵌入页面证据，不能贸然删掉导致漏挂载。小窗口会按既有 CSS 断点隐藏部分装饰，放大窗口后再看；并未把这些待确认项记作已修复。完整说明见 [Issue #1 回执](https://github.com/lanmengSakura/diana-multi-app-launcher/blob/main/docs/ISSUE_1_RECEIPT.md)。

Cursor 3.17.21 的系统浅色/手动日夜/恢复，以及 Grok Bot 0.28.0 的核心循环属于 rc.2 的既有测试记录，不是本次重新测试。ZCode 继续限定已验证版本；VS Code 默认是颜色主题；Harness 仍须自行完成对应源码部署、依赖、构建和模型配置。

本作品为非商业粉丝二创，与相关官方无隶属或背书关系。MIT 只覆盖代码，不覆盖角色美术及歌曲录音。
