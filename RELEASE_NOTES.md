# Diana 多合一启动器 · Beta.4 RC.3

本次为公开测试更新，新增本机应用关联，保留定稿美术、完整运行组件和《Hopeful Dreamer》本地循环配乐。

## 完整功能，由你知情选择

可以自行选择是否使用本启动器。公开包提供完整挂载组件，但下载、打开或关联应用位置不代表同意开启调试连接。Codex、Cursor、Grok Bot、ZCode 新开实验调试会话前会说明风险并等待确认，取消不执行本次挂载。

**CDP 回环端口没有身份认证。** 同机进程可能读取当前可见界面、执行渲染脚本或截图；仅撤下皮肤或关闭启动器不会关闭目标应用持有的端口，必须完整退出该目标应用。请在信任的本机环境使用，并阅读 [完整风险说明](https://github.com/lanmengSakura/diana-multi-app-launcher/blob/main/SECURITY.md)。

## 更新内容

- 在状态栏右侧新增小号“关联”按钮，不挤占原有状态信息；应用列表折叠时也可使用。
- 自动识别不到应用时，可浏览或粘贴程序位置并保存；下次打开仍保留。路径失效时提示重新关联，不默默改用另一份安装。
- “恢复自动识别”只清除当前应用的手动路径，不删除应用、主题或其他关联；取消/选错/保存失败不清空旧设置。
- Codex 仍按 Windows 官方安装包自动定位，避免固定旧版本目录；DeepSeek Harness 选择已部署的项目根目录。
- 修正 Codex 曾保存过原生配色后，新开会话可能跳过风险提示的问题。位置关联不会记住或代替调试授权。
- 完整运行组件沿用 rc.2，未放宽版本、签名、哈希和进程归属检查；不创建 watcher、服务、计划任务或自启动。

## 下载与更新

- 安装版：`Diana-Multi-App-Launcher_0.1.0-beta.4-rc.3_x64-setup.exe`。
- 便携版：`diana-multi-app-launcher_0.1.0-beta.4-rc.3_x64-portable.exe`。
- 核对随附 `SHA256SUMS-launcher.txt`；两种 EXE 均未签名。更新前正常关闭旧启动器，保留旧文件以便回退。旧版不会自动更新。
- 本次不迁移旧 Cursor/Grok 登记，不删除旧恢复记录，不更改目标应用设置。已登记适配器的恢复周期仍按原入口完成。
- 选择应用 → 状态栏旁“关联”（需要时）→ 保存路径 → 关闭设置 → 点击主按钮并自行决定是否同意风险提示。

## 检查范围与限制

本次包含 TypeScript/生产构建、资源哈希与编译载荷检查、48 项 Rust 测试（另有 1 项真实应用 smoke 明确忽略），以及关联界面和四个目标的风险提示取消检查。路径读写测试使用独立临时目录，界面检查使用模拟 IPC；没有操作已打开的目标应用。

Windows 原生文件选择框尚未真机点选，仍支持粘贴路径。安装包生成不等于已在干净机器完成安装/卸载验收；也不等于八个目标在所有版本上都能挂载。本次未新增目标应用的真实挂载/日夜/恢复验收。

Cursor 3.17.21 的系统浅色/手动日夜/恢复，以及 Grok Bot 0.28.0 的核心循环属于 rc.2 的既有测试记录，不是本次重新测试。ZCode 继续限定已验证版本；VS Code 默认是颜色主题；Harness 仍须自行完成对应源码部署、依赖、构建和模型配置。

本作品为非商业粉丝二创，与相关官方无隶属或背书关系。MIT 只覆盖代码，不覆盖角色美术及歌曲录音。
